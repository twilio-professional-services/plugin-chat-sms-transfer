{
  "name": "plugin-chat-sms-transfer-functions",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run --env"
  },
  "devDependencies": {
    "twilio-run": "^3.2.0"
  },
  "dependencies": {
    "twilio-flex-token-validator": "^1.5.6"
  }
}
